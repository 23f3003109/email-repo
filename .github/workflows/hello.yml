name: Hello Action  # Name of your workflow (shows in GitHub UI)

on: [push]  # Triggers on any push to the repo (easiest for testing)

jobs:
  test:
    runs-on: ubuntu-latest  # Free virtual machine (runner) to run on[web:1]
    steps:
      - name: 23f3003109@ds.study.iitm.ac.in  # Your required step name
        run: echo "Hello, world!"  # Simple command that runs[web:1]
